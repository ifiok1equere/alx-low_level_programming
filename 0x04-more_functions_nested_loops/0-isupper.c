#include <ctype.h>
#include "main.h"

/**
    *_isupper - checks for lowercase
      *@c: character parameter
        *Description - checks uppercase
	  *Return: retrns 0
	  */

int _isupper(int c)
{
	  int d = 0, e = 1;

	  if (isupper(c))
	  {
		  return (e);
	  }
	  else
	  {
		  return (d);
	  }
}
