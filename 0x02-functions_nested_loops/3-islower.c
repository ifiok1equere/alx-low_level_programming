#include <ctype.h>
#include "main.h"

/**
  *_islower(int c) - checks for lowercase
  *Description - checks lowercase
  *Return: retrns 0
*/

int _islower(int c)
{
	int d = 0;
	int e = 1;

	if (islower(c))
	{
		return (e);
	}
	else
	{
		return (d);
	}
}

